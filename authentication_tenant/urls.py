from django.conf.urls import patterns, include, url
from authentication_tenant import views


urlpatterns = (
    # url(r'^activation_required$', views.activation_required_page, name='activation_required'),
    # url(r'^registration$', views.registration_stage_1_page, name='registration_stage1'),
    # url(r'^registration/pre-intake$', views.registration_stage_2_page, name='registration_stage2'),
    # url(r'^registration/intake$', views.registration_stage_3_page, name='registration_stage3'),
)
