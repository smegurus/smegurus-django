from django.conf.urls import include, url
from tenant_workspace_exercise import views


urlpatterns = (
    # CLIENTS
    # url(r'^workspace/(.*)/exercise/(.*)/question/(.*)/$', views.client_detail_page, name='tenant_workspace_exercise_client_detail'),
    # url(r'^workspace/(.*)/exercise/(.*)/$', views.client_master_page, name='tenant_workspace_exercise_client_master'),
)
